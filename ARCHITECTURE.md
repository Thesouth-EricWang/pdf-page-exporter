# 架构与设计决策

> 这份文档给**未来的你**看。最重要的不是模块图，是**为什么这么设计**。
> 一年后你会忘记当初的取舍理由，而它决定了你还能不能放心改。

---

## 一、模块划分

```
src/
├── app.h        公共声明：数据结构、自定义消息 ID、函数原型
├── core.cpp     PDFium 动态绑定 + 导出逻辑 + Word 转换 + GDI+ 编码
├── main.cpp     Win32 界面：控件、拖放、后台线程
└── version.rc   版本资源（文件属性里能看到的那些信息）
```

职责边界：

- **`core.cpp` 不认识窗口**。它只通过 `Ctx::Log` 投递文本；`hwnd` 为空时改打到 stdout（这让无界面测试成为可能）。改核心逻辑时不要往里加界面相关的东西。
- **`main.cpp` 不碰 PDF 细节**。它只负责收集参数、启动工作线程、显示日志和汇总。改界面时不要绕过 `Params` 结构直接调 PDFium。

界面和核心之间只有两个自定义消息：

| 消息 | wparam | 含义 |
|---|---|---|
| `WM_APP_LOG` | `new std::wstring*` | 一行日志，界面负责释放 |
| `WM_APP_LOGERR` | `new std::wstring*` | 错误日志（红色） |
| `WM_APP_DONE` | `new DoneInfo*` | 整批完成，界面负责释放 |

**内存所有权规则**：谁 `new` 谁不管，投递出去就交给界面释放。这是跨线程唯一允许的方式。

---

## 二、关键决策

### 决策 1：原生 C++，不用 Python

**背景**：初版是 Python + tkinter + PyMuPDF，功能完全没问题，还带了拖放、批量、压缩档。

**转折点**：要分享给别人时，PyInstaller 打出来 **53 MB**，而且 `--windowed` 下窗口还不显示。需求是「几 MB、纯原系统能跑」。

**选了**：原生 Win32 + 静态 CRT（`/MT`）。

**代价**：写起来慢得多，界面控件要手摆坐标，没有现成库。

**收益**：exe 218 KB，加 PDFium 共 **7.2 MB**；零依赖；Win7 直接双击。

---

### 决策 2：exe + dll 两个文件，坚决不做自解压单 exe

**背景**："单 exe"听起来更方便发。

**没选单 exe**：自解压必然在运行时往 `%TEMP%` 吐文件。这正是"残留"的来源——PyInstaller 的 `_MEIxxxx` 目录就是活证，强杀或崩溃后它会留在那里。

**代价**：多一个文件，对方必须放同一个目录（`使用说明.txt` 里写死了这条）。

**收益**：运行时**零解压、零残留**。这是"绿色软件"这个承诺的真正兑现。

---

### 决策 3：扫描页直接抽内嵌 JPEG，不走渲染

**背景**：实际要处理的证明全是扫描件，每页就是一整张 400 dpi 的 JPEG。

**选了**：满足下面全部条件时，用 `FPDFImageObj_GetImageDataRaw` 把原始 JPEG 字节直接落盘：

1. 用户选了 JPG
2. 压缩档 ≥ 90
3. 页面只有 1 个图像对象
4. 该对象覆盖 ≥ 95% 页面
5. 滤镜是 `DCTDecode`

**收益**：**无损**（输出与源内嵌图逐字节一致，1 020 087 字节对得上），而且快得多——省掉解码+重编码。

**代价/为什么有第 2 条**：压缩档 < 90 时走渲染。因为用户选低压缩档就是明确要"压小"，直接抽原图就违背这个意图了。

---

### 决策 4：PDFium 用 `GetProcAddress` 动态加载

**没链导入库**（虽然 `pdfium.dll.lib` 就在包里）。

**收益**：缺 `pdfium.dll` 时程序照常启动，只是提示"扫描件能用，其他不行"；而不是加载失败整个起不来。

**代价**：每个函数都要 `p_` 前缀调用。直接写 `FPDF_XXX` 会链接失败——见雷区第 1 条。

---

### 决策 5：用 GDI+ 编码，不用 WIC

两者都在 Win7+ 系统里自带。选 GDI+ 是因为 `Bitmap.Save` 直接支持质量参数，代码量大约是 WIC 的三分之一（WIC 要自己建格式转换器 + 属性包）。

---

### 决策 6：递归删目录用纯 Win32

**背景**：一开始用了 `SHFileOperation`，标准做法。

**问题**：临时目录**删不掉，而且不报错**。

**原因**：`SHFileOperation` 依赖 COM 初始化。无界面测试驱动里没调 `CoInitialize`，它就静默不干活。

**改成**：`FindFirstFileW` / `FindNextFileW` + `DeleteFileW` + `RemoveDirectoryW` 自己递归，不依赖任何 shell/COM 状态。

**教训**：这也是"静默失败"的典型——返回值没问题，日志没问题，就是目录还在。

---

### 决策 7：Word 转换的参数写进 .ps1，不走环境变量

**背景**：`CreateProcessW` 传自定义环境块给 PowerShell，结果返回 `error 87`（ERROR_INVALID_PARAMETER）。

**改成**：把源路径和目标路径直接拼进临时 `.ps1`，`CreateProcessW` 的 `lpEnvironment` 传 `nullptr` 继承当前环境。

**附带必须做的**：`.ps1` 存成 **UTF-8 带 BOM**。PowerShell 5.1 读无 BOM 的 UTF-8 会按系统代码页解析，中文路径直接乱码。

---

## 三、数据流

```
输入文件
  │
  ├─ 扩展名是 .pdf ────────────────► 直接用
  │
  └─ 是 Word (.docx/.doc/.docm/.rtf/.odt)
        └─ 写临时 w2p.ps1 → 调 Word COM 另存为临时 PDF
              └─► 用临时 PDF，导出结束后连同整个临时目录删除
  │
  ▼
PDFium 打开文档 → 取页数 → 解析页码范围
  │
  ▼
逐页：
  ├─ 分析页对象：找最大图像对象，读它的 horizontal_dpi 和覆盖率
  ├─ 定 dpi：手填值 或 自动（用图像对象的实际 dpi，没有则 300，夹在 72-1200）
  └─ 二选一：
       ├─ 无损提取原始 JPEG 字节
       └─ 按 dpi 渲染到 BGRA 缓冲 → GDI+ 编码为 JPG/PNG
  │
  ▼
写到 <输出目录>/<文件名>/<文件名>-第N页.<ext>
```

**关键中间产物的生命周期**：

| 产物 | 位置 | 何时删 |
|---|---|---|
| 临时 PDF | `%TEMP%\pdfex_<tick>\` | 该文件导出流程结束时 |
| `w2p.ps1` | 同上 | PowerShell 返回后立刻 |
| 整个临时目录 | 同上 | 同上，用纯 Win32 递归删除 |

---

## 四、已知限制

- **处理 Word 需要本机装 Microsoft Word**（走 COM）。没装会明确报错。
- **PDFium 占 7 MB，无法再小**。PDF 渲染必须有引擎，纯原装 Windows 里没有。想要 KB 级就只能做"只抽扫描图"的阉割版。
- **32 位和 64 位不通用**。`exe` 和 `pdfium.dll` 必须同架构。
- **不支持加密 PDF**。加密文档会报"打不开"。
- 扫描页的 dpi 上限被夹在 1200，防止异常文件炸内存。

---

## 五、改代码时的雷区

1. **所有 PDFium 调用必须走 `p_XXX` 指针**。直接写 `FPDF_XXX` 会链接失败，因为没链导入库。
2. **调 PDFium 前必须先 `LoadPdfium()`**，否则所有指针是空的。
3. **源文件含中文，编译必须带 `/utf-8`**。不加会被按 GBK 解析，把代码结构吃坏，报莫名其妙的"未声明的标识符"。
4. **版本号写了两个地方**：`src/main.cpp` 的 `APP_VERSION` 和 `src/version.rc`。改版本要同时改，否则标题栏和文件属性会对不上。
5. **`build.bat` 必须用 `rc.exe` 单独编译 `.rc`**。`cl` 不总是把 `.rc` 转给 `rc.exe`，会直接丢给链接器报 `LNK1107`。
6. **`build.bat` 里的注释只能是英文**。cmd.exe 按 OEM 代码页读批处理，UTF-8 中文会让命令解析错乱。

---

## 六、依赖

| 依赖 | 版本 | 用途 | 许可证 | 能否替换 |
|---|---|---|---|---|
| PDFium | chromium 最新 | PDF 渲染 + 图像抽取 | BSD-3-Clause | MuPDF（更大）；无法完全去掉 |
| GDI+ | 系统自带 | JPEG / PNG 编码 | 系统组件 | WIC（代码更多） |
| user32 / gdi32 / comctl32 / comdlg32 / shell32 / ole32 | 系统自带 | 界面、文件对话框、路径 | 系统组件 | 无 |
| MSVC 运行库 | 静态链接（`/MT`） | 运行时 | 随编译器 | 无（静态链避免依赖） |
